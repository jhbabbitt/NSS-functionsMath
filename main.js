const addNum = (numOne, numTwo) => {
    sum = numOne + numTwo
    return sum
}

const doubleNum = (numThree) => {
    product = numThree * 2
    return product
}
let integer = addNum(5,7)
let finalAnswer = doubleNum(integer)
console.log(finalAnswer)